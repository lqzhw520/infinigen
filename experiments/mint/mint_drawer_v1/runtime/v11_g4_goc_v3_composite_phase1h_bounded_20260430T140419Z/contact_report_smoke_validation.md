# Contact Report Smoke Validation

positive_validator_accepted: True
body_based_rejected: True
legacy_name_only_rejected: True

The authority validator accepts exact-ID schema alignment, but Stage 4 still fails because forbidden reset contacts are non-empty and reset physics is invalid.
