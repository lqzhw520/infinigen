# Execution Plan

Task: `V11_G4_GOC_V3_COMPOSITE_PHASE1H_BOUNDED_AUTONOMOUS_RUN_V2`

This run is a harness-gated bounded runtime science phase. It creates and binds a composite task spec, reviews reset contact physics under GOC-v3 exact IDs, performs minimal repair only for a proven stage-local blocker, runs preaction smoke, and only then attempts bounded Phase1H candidate execution. It does not ask for user prompts mid-run.

No direct `current_truth.json` or `next_actions.json` mutation is allowed. No training, fine-tuning, MINT vendor edits, body-based authority, legacy name-only authority, direct-qpos drawer opening, or visual success claim is allowed.
