# V11-G4 GOC-v3 Route Repair Execution Plan

This phase is telemetry-driven route repair, not harness repair, GOC-v3 rebuild, reset geometry repair, rollout success, render success, or training. It binds the route repair task spec in the production lock, replays prior no-target-contact evidence to recover exact contact pairs, then runs clean-HEAD diagnostic route probes with per-step EEF, legal-pad, handle, contact, gripper command, and drawer traces. Any positive candidate-producing attempt must run from a clean committed tree.
